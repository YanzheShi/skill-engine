# --- Python ---
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg
*.whl
*.so
*.pyd

# --- Virtual environments ---
.venv/
venv/
env/
.venv.bak/

# --- IDE ---
.claude/
.idea/
.vscode/
*.swp
*.swo
*~

# --- Testing ---
.pytest_cache/
.coverage
coverage/
htmlcov/
*.coverage.*
.tox/

# --- Linting ---
.ruff_cache/
.mypy_cache/
.dmypy.json
dmypy.json

# --- OS ---
.DS_Store
Thumbs.db
*.log

# --- Project specific ---
# uv lock file is auto-generated
uv.lock

# Generated / temp directories
.ai/
analysis_output/
output/
${ANALYSIS_OUTPUT_DIR}/
fix_indent.py
.workbuddy/

# --- Runtime artifacts (never commit) ---
.skill_engine_snapshots/
pastes/
sessions/
junit.xml
junit_full.xml
demo/
logs_*.log
_approval_debug.log
bt.log
p6.log
pt_result.log

# MOA 自动检查点（每轮落盘，正常结束自动删除；崩溃后用于 --resume-from）
moa_session_state.json

# Isolated venv used by some scripts (already self-ignored, belt-and-suspenders)
active/

# .env files may contain API keys
.env
.env.*
!.env.example

# config.yml 是统一配置源，可能含明文密钥（取代分散的 .env 与 models.yaml）
config.yml
config.yml.*
!config.yml.example

# 旧式模型配置文件（已被 config.yml 取代，保留忽略以防残留）
models.yaml
models.yaml.*
!models.yaml.example

# Skill install target
~/.skills/

tests/__pycache__/