# 📥 Context Input — Stage 1 上下文投放模板

> **使用说明**：在每轮学习会话的 Stage 1，按此模板提供上下文。
> 可以全部填写，也可以只填关键项（带 ⭐ 为必填）。

---

## ⭐ 目标模块

**模块/文件名称**：  
**文件路径**：  
**主要语言/框架**：  

---

## ⭐ 模块职责（一句话）

> 这个模块/文件是干嘛的？

---

## 扩展角色声明（v2 新增）

> 选择本轮需要启用的扩展审计角色（可多选、可不选）

- [ ] 🧪 **测试视角官** — 覆盖率审计、边界用例、可测试性审查
- [ ] 🔧 **SRE 运维视角官** — 可观测性、告警、灾备、部署安全
- [ ] 🛡️ **安全审计官** — 注入、越权、数据泄露、供应链安全

> 💡 如果不勾选，只走核心 5 阶段（讲解→复述→追问→评审→沉淀）。
> 全选则走完整 8 阶段流程。

---

## 已有测试情况（如启用测试角色）

- **测试文件**：`<path>` / 无
- **覆盖情况**：无测试 / 仅 Happy Path / 部分边界 / 较完整
- **测试框架**：`<go test / pytest / jest / ...>`

---

## 已有运维/SRE 情况（如启用 SRE 角色）

- **日志方案**：`<如 zap + ELK>` / 无
- **监控方案**：`<如 Prometheus + Grafana>` / 无
- **链路追踪**：`<如 Jaeger / OpenTelemetry>` / 无
- **部署方式**：`<如 K8s / Docker / 裸机>`

---

## 已有安全关注（如启用安全角色）

- **认证方式**：`<如 JWT / Session / OAuth>` 
- **敏感数据处理**：`<如加密存储 / 脱敏日志>` / 未处理
- **已知安全顾虑**：`<如"担心 SQL 注入""担心越权访问">` / 无

---

## 业务背景

- **所属业务场景**：（如"订单支付流程中的库存锁定"）
- **上游调用方**：（谁触发这个模块）
- **下游依赖**：（它调用谁、读写哪些存储）
- **关键业务规则**：（如"库存不能超卖""订单 30 分钟未支付自动取消"）

---

## 已知约束

| 类型 | 约束内容 |
|---|---|
| 性能 | QPS 峰值 / 延迟要求 / 数据量级 |
| 一致性 | 强一致 / 最终一致 / 允许丢失 |
| 部署 | 单实例 / 多副本 / 跨地域 |
| 技术栈 | 语言版本 / 关键中间件 / 框架限制 |
| 团队 | 人数 / 迭代节奏 / on-call 压力 |
| 其他 | 任何你认为是"不可变条件"的限制 |

---

## 重点关注方向（可选）

> 你希望本轮重点攻克哪些方面？

- [ ] 核心逻辑理解
- [ ] 并发/线程安全
- [ ] 数据一致性
- [ ] 性能瓶颈
- [ ] 错误处理与降级
- [ ] 可测试性
- [ ] 可扩展性
- [ ] 与其他模块的交互
- [ ] 面试表达训练
- [ ] 其他：______

---

## 代码片段（可选但推荐）

> 直接粘贴关键代码，或提供文件路径让 AI 读取。
> 建议包含：
> - 入口函数
> - 核心逻辑（关键 50～200 行）
> - 相关配置/接口定义

```go
// 粘贴代码 here
```

---

## 我当前的困惑/想搞懂的点

> 用自然语言写下你不确定的地方，这会让讲解者更有针对性。

- 
- 
- 

---

## 提交方式

填写完毕后，直接发送给 AI 并说：

```
以上是我的上下文，请进入 Stage 2（代码讲解者模式）。
```
