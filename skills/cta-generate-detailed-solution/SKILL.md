---
name: cta-generate-detailed-solution
description: 给定一道编程题的完整描述（description），生成详细、可教学的题解文档。覆盖从暴力到最优的多条思路，每条思路含核心思想、算法流程、复杂度分析与可运行代码；并附思路对比表、关键词与核心洞察、易错点、推荐解法。供 code-tutor-agent 在用户请求"详细题解/讲题/讲讲思路"时调用，区别于仅输出解题代码的 cta-generate-solution。
when_to_use: 当 code-tutor-agent 已有一道编码题、需要生成面向教学的详细题解（而非仅解题代码）时使用；输入为整题的 description 文本（经 $ARGUMENTS 注入）。
arguments:
- description
---

# cta-generate-detailed-solution

你是一位资深算法竞赛教练。下面是一道编程题的完整描述：

$ARGUMENTS

请基于该题目描述，生成一份**详细、可教学**的题解文档（Markdown）。目标是让读者不仅拿到代码，更能理解"为什么这么想、有哪些坑、各思路如何权衡"。

## 输出要求

严格按以下 Markdown 结构输出，**不要**输出任何额外解释、前言或总结，也不要输出你的思考过程（chain-of-thought）。每一级标题层级必须如上，**代码块必须放在 ```` ```python ```` 围栏内**，且都可被 `compile` 并通过该题测试用例。

# 题目描述
<基于 $ARGUMENTS 重述题目要点：输入、输出、约束、目标。自洽可独立理解。>

# 思路一：<思路名称，通常是最自然的暴力/朴素想法>
## 解题思路
**核心思想**：<一句话概括>
**算法流程**：
1. <步骤一>
2. <步骤二>
3. <步骤三>
## 复杂度
- 时间复杂度：<分析>
- 空间复杂度：<分析>
## Code
```python
class Solution:
    def methodName(self, ...):
        ...
```
**问题/优化方向**：<该思路的瓶颈或可改进点>

---

# 思路二：<优化思路名称>
（结构同上：## 解题思路 / ## 复杂度 / ## Code / **问题/优化方向**）

---

# 思路三：<最优解名称，若存在>
（结构同上）

---

# 总结对比
| 思路 | 时间复杂度 | 空间复杂度 | 特点 |
|------|-----------|-----------|------|
| 思路一（暴力） | ... | ... | ... |
| 思路二 | ... | ... | ... |
| 思路三（最优） | ... | ... | ... |

---

# 关键词与核心洞察
## 关键词
- <关键词1>：<为什么重要 / 学到了什么>
- <关键词2>：<为什么重要 / 学到了什么>
## Insight
<对算法本质的深度理解，超越实现细节；记录"恍然大悟"的认知升级>
## 易错点
- <实现中容易出错的细节 / 边界情况>

---

**推荐**：在面试/实战中使用**<思路X>**，**<理由>**。

## 内容要求
- 至少给出 2 条思路（暴力 → 最优）；若题目简单可只给 2 条，但必须覆盖从"最自然想法"到"最优解"的演进。
- 每个思路的 Code 必须是合法可运行的 Python `class Solution`，方法签名与题目描述一致。
- 必须记录"为什么从思路一演进到思路二"（瓶颈分析），不要只罗列成功解法。
- 复杂度必须给出推导，而非凭空写 O(n)。
- 易错点要具体到实现细节（如指针更新顺序、循环结束后的边界检查）。
